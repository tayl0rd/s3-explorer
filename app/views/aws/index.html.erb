<% @directories.each do |directory| %>
  <div>
    <i class="fa fa-folder-o"></i>
    <%= link_to directory, directory_path(directory) %>
  </div>
<% end %>

<% @contents.each do |object| %>
  <div>
    <%= link_to object, signed_url(title,object) %>
  </div>
<% end %>



